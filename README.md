# Memory game

Voice controlled game of memory.

# Run in development

Install node modules:

`npm install`

and run locally:

`npm start`

Game starts on localhost:9000

# Build for production

Run:

`npm build`

Your static build is in `/dist` folder. Deploy anywhere.
